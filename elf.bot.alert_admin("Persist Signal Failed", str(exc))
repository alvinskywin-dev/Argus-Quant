[1mdiff --git a/app/main.py b/app/main.py[m
[1mindex ef4f232..7d1fa66 100644[m
[1m--- a/app/main.py[m
[1m+++ b/app/main.py[m
[36m@@ -9,6 +9,8 @@[m [mBoots PostgreSQL schema, starts:[m
     - FastAPI dashboard[m
 [m
 …all as supervised asyncio tasks. Handles graceful shutdown on SIGTERM/SIGINT.[m
[32m+[m
[32m+[m[32mStartup self-diagnostics verify all critical connections before serving.[m
 """[m
 from __future__ import annotations[m
 [m
[36m@@ -28,7 +30,8 @@[m [mfrom app.market_data import ([m
     universe,[m
     universe_loop,[m
 )[m
[31m-from app.market_data.cache import shutdown_redis[m
[32m+[m[32mfrom app.market_data.binance_client import get_client[m
[32m+[m[32mfrom app.market_data.cache import get_redis, shutdown_redis[m
 from app.market_data.ws_engine import ws_price_loop[m
 from app.scanner import MarketScanner, SignalTracker[m
 from app.telegram_bot import TelegramBot[m
[36m@@ -36,6 +39,93 @@[m [mfrom app.utils.helpers import run_forever[m
 from app.utils.logger import logger[m
 [m
 [m
[32m+[m[32m# ---------- startup diagnostics ----------[m
[32m+[m
[32m+[m[32masync def _check_binance() -> bool:[m
[32m+[m[32m    try:[m
[32m+[m[32m        client = await get_client()[m
[32m+[m[32m        info = await client.exchange_info()[m
[32m+[m[32m        syms = [s for s in info.get("symbols", []) if s.get("status") == "TRADING"][m
[32m+[m[32m        logger.info(f"✅ Binance OK — {len(syms)} active symbols")[m
[32m+[m[32m        return True[m
[32m+[m[32m    except Exception as exc:[m
[32m+[m[32m        logger.error(f"❌ Binance FAILED: {exc}")[m
[32m+[m[32m        return False[m
[32m+[m
[32m+[m
[32m+[m[32masync def _check_database() -> bool:[m
[32m+[m[32m    try:[m
[32m+[m[32m        await init_db()[m
[32m+[m[32m        cnt = await repo.count_signals_since([m
[32m+[m[32m            __import__("datetime").datetime.now(__import__("datetime").timezone.utc)[m
[32m+[m[32m            - __import__("datetime").timedelta(days=30)[m
[32m+[m[32m        )[m
[32m+[m[32m        logger.info(f"✅ Database OK — {cnt} signals in last 30d")[m
[32m+[m[32m        return True[m
[32m+[m[32m    except Exception as exc:[m
[32m+[m[32m        logger.error(f"❌ Database FAILED: {exc}")[m
[32m+[m[32m        return False[m
[32m+[m
[32m+[m
[32m+[m[32masync def _check_redis() -> bool:[m
[32m+[m[32m    try:[m
[32m+[m[32m        r = await get_redis()[m
[32m+[m[32m        await r.ping()[m
[32m+[m[32m        logger.info("✅ Redis OK")[m
[32m+[m[32m        return True[m
[32m+[m[32m    except Exception as exc:[m
[32m+[m[32m        logger.warning(f"⚠️  Redis FAILED (non-fatal, cache disabled): {exc}")[m
[32m+[m[32m        return True  # non-fatal[m
[32m+[m
[32m+[m
[32m+[m[32masync def _check_telegram(bot: TelegramBot) -> bool:[m
[32m+[m[32m    if not settings.telegram_bot_token:[m
[32m+[m[32m        logger.warning("⚠️  TELEGRAM_BOT_TOKEN not set — Telegram disabled")[m
[32m+[m[32m        return True  # non-fatal, bot just won't send[m
[32m+[m[32m    if bot.app is None:[m
[32m+[m[32m        logger.warning("⚠️  Telegram app not initialized yet")[m
[32m+[m[32m        return True[m
[32m+[m[32m    try:[m
[32m+[m[32m        me = await bot.app.bot.get_me()[m
[32m+[m[32m        logger.info(f"✅ Telegram OK — @{me.username}")[m
[32m+[m[32m        return True[m
[32m+[m[32m    except Exception as exc:[m
[32m+[m[32m        logger.error(f"❌ Telegram FAILED: {exc}")[m
[32m+[m[32m        return False[m
[32m+[m
[32m+[m
[32m+[m[32masync def _startup_report(bot: TelegramBot) -> None:[m
[32m+[m[32m    """Print startup summary and fail loudly on critical errors."""[m
[32m+[m[32m    logger.info("=" * 60)[m
[32m+[m[32m    logger.info("  ALPHA RADAR SIGNALS — STARTUP DIAGNOSTICS")[m
[32m+[m[32m    logger.info("=" * 60)[m
[32m+[m
[32m+[m[32m    binance_ok = await _check_binance()[m
[32m+[m[32m    db_ok = await _check_database()[m
[32m+[m[32m    redis_ok = await _check_redis()[m
[32m+[m[32m    tg_ok = await _check_telegram(bot)[m
[32m+[m
[32m+[m[32m    logger.info("-" * 60)[m
[32m+[m[32m    logger.info(f"  Binance:   {'OK' if binance_ok else 'FAILED'}")[m
[32m+[m[32m    logger.info(f"  Database:  {'OK' if db_ok else 'FAILED'}")[m
[32m+[m[32m    logger.info(f"  Redis:     {'OK' if redis_ok else 'DEGRADED (no cache)'}")[m
[32m+[m[32m    logger.info(f"  Telegram:  {'OK' if tg_ok else 'FAILED'}")[m
[32m+[m[32m    logger.info(f"  Timeframes: {', '.join(settings.timeframes)}")[m
[32m+[m[32m    logger.info(f"  Scan interval: {settings.scan_interval_sec}s")[m
[32m+[m[32m    logger.info(f"  Min confidence: {settings.min_confidence}%")[m
[32m+[m[32m    logger.info(f"  Min RR: 1:{settings.min_rr}")[m
[32m+[m[32m    logger.info(f"  Max signals/hr: {settings.max_signals_per_hour}")[m
[32m+[m[32m    logger.info(f"  Dashboard port: {settings.dashboard_port}")[m
[32m+[m[32m    logger.info("=" * 60)[m
[32m+[m
[32m+[m[32m    if not db_ok:[m
[32m+[m[32m        raise RuntimeError("Database connection failed — cannot start")[m
[32m+[m[32m    if not binance_ok:[m
[32m+[m[32m        raise RuntimeError("Binance connection failed — cannot start")[m
[32m+[m
[32m+[m
[32m+[m[32m# ---------- application ----------[m
[32m+[m
 class App:[m
     def __init__(self) -> None:[m
         self.bot = TelegramBot()[m
[36m@@ -47,59 +137,61 @@[m [mclass App:[m
 [m
     async def _handle_signal(self, sig: dict) -> None:[m
         """Called by the scanner whenever a new signal is emitted."""[m
[31m-        # Persist first — gets the id back[m
[31m-        db_fields = {k: v for k, v in sig.items() if not k.startswith("_")}[m
[31m-        # Strip non-column fields[m
[31m-        db_fields.pop("status", None)[m
[31m-        db_fields["status"] = "OPEN"[m
         try:[m
             persisted = await repo.create_signal({[m
[31m-                "symbol": sig["symbol"],[m
[31m-                "side": sig["side"],[m
[31m-                "timeframe": sig["timeframe"],[m
[31m-                "confidence": sig["confidence"],[m
[31m-                "risk_level": sig["risk_level"],[m
[31m-                "strategy": sig["strategy"],[m
[31m-                "reasons": sig["reasons"],[m
[31m-                "entry_low": sig["entry_low"],[m
[31m-                "entry_high": sig["entry_high"],[m
[31m-                "tp1": sig["tp1"],[m
[31m-                "tp2": sig["tp2"],[m
[31m-                "tp3": sig["tp3"],[m
[31m-                "stop_loss": sig["stop_loss"],[m
[32m+[m[32m                "symbol":      sig["symbol"],[m
[32m+[m[32m                "side":        sig["side"],[m
[32m+[m[32m                "timeframe":   sig["timeframe"],[m
[32m+[m[32m                "confidence":  sig["confidence"],[m
[32m+[m[32m                "risk_level":  sig["risk_level"],[m
[32m+[m[32m                "strategy":    sig["strategy"],[m
[32m+[m[32m                "reasons":     sig["reasons"],[m
[32m+[m[32m                "entry_low":   sig["entry_low"],[m
[32m+[m[32m                "entry_high":  sig["entry_high"],[m
[32m+[m[32m                "tp1":         sig["tp1"],[m
[32m+[m[32m                "tp2":         sig["tp2"],[m
[32m+[m[32m                "tp3":         sig["tp3"],[m
[32m+[m[32m                "stop_loss":   sig["stop_loss"],[m
                 "risk_reward": sig["risk_reward"],[m
[31m-                "status": "OPEN",[m
[32m+[m[32m                "status":      "OPEN",[m
             })[m
         except Exception as exc:  # noqa: BLE001[m
[31m-            logger.exception(f"persist signal failed: {exc}")[m
[32m+[m[32m            logger.exception(f"💾 persist signal FAILED {sig.get('symbol')}: {exc}")[m
             await self.bot.alert_admin("Persist Signal Failed", str(exc))[m
             return[m
 [m
[32m+[m[32m        logger.info([m
[32m+[m[32m            f"💾 signal #{persisted.id} SAVED  "[m
[32m+[m[32m            f"{sig['symbol']} {sig['side']} "[m
[32m+[m[32m            f"conf={sig['confidence']}% rr=1:{sig['risk_reward']}"[m
[32m+[m[32m        )[m
[32m+[m
         try:[m
             sent_messages = await self.bot.broadcast_signal(sig)[m
         except Exception as exc:  # noqa: BLE001[m
[31m-            logger.exception(f"broadcast signal failed: {exc}")[m
[32m+[m[32m            logger.exception(f"📤 broadcast FAILED #{persisted.id}: {exc}")[m
             await self.bot.alert_admin("Broadcast Signal Failed", str(exc))[m
             sent_messages = [][m
 [m
         if sent_messages:[m
[31m-            # keep legacy first message id for backward compatibility[m
             await repo.update_signal([m
                 persisted.id,[m
                 {"telegram_message_id": sent_messages[0]["message_id"]},[m
             )[m
[31m-[m
             for item in sent_messages:[m
                 await repo.save_signal_message([m
                     persisted.id,[m
                     item["chat_id"],[m
                     item["message_id"],[m
                 )[m
[31m-[m
[31m-        logger.info([m
[31m-            f"🚀 SIGNAL #{persisted.id} {sig['symbol']} {sig['side']} "[m
[31m-            f"conf={sig['confidence']}% rr=1:{sig['risk_reward']}"[m
[31m-        )[m
[32m+[m[32m            logger.info([m
[32m+[m[32m                f"📤 signal #{persisted.id} BROADCAST to {len(sent_messages)} chat(s)"[m
[32m+[m[32m            )[m
[32m+[m[32m        else:[m
[32m+[m[32m            logger.warning([m
[32m+[m[32m                f"📤 signal #{persisted.id} NOT broadcast — "[m
[32m+[m[32m                "check TELEGRAM_SIGNAL_CHAT_ID and tier routing config"[m
[32m+[m[32m            )[m
 [m
     async def _handle_tracker_event(self, payload: dict) -> None:[m
         await self.bot.broadcast_event(payload)[m
[36m@@ -118,14 +210,13 @@[m [mclass App:[m
 [m
     async def start(self) -> None:[m
         logger.info("=== AI Futures Signal System booting ===")[m
[31m-        await init_db()[m
[32m+[m
[32m+[m[32m        await self.bot.start()[m
[32m+[m[32m        await _startup_report(self.bot)[m
         await universe.refresh()[m
 [m
         self.tracker.on_update(self._handle_tracker_event)[m
 [m
[31m-        await self.bot.start()[m
[31m-[m
[31m-        # Run everything as supervised forever-tasks[m
         self._tasks = [[m
             asyncio.create_task(run_forever(universe_loop, name="universe")),[m
             asyncio.create_task(run_forever(self.scanner.run_forever, name="scanner")),[m
[36m@@ -133,7 +224,12 @@[m [mclass App:[m
             asyncio.create_task(run_forever(ws_price_loop, name="ws_price")),[m
             asyncio.create_task(self._run_dashboard()),[m
         ][m
[31m-        logger.info("=== all services running ===")[m
[32m+[m
[32m+[m[32m        logger.info([m
[32m+[m[32m            f"=== all services running  "[m
[32m+[m[32m            f"universe={len(universe.symbols)} symbols  "[m
[32m+[m[32m            f"dashboard=:{settings.dashboard_port} ==="[m
[32m+[m[32m        )[m
         await self._stopping.wait()[m
 [m
     async def stop(self) -> None:[m
[36m@@ -149,8 +245,6 @@[m [mclass App:[m
             t.cancel()[m
 [m
         await asyncio.gather(*self._tasks, return_exceptions=True)[m
[31m-[m
[31m-        # allow pending aiohttp requests to settle[m
         await asyncio.sleep(0.5)[m
 [m
         await self.bot.shutdown()[m
